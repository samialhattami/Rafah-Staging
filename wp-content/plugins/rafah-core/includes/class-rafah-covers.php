<?php
/**
 * Rafah Core — REMOVED: Status-based project covers.
 *
 * The "Status Covers" feature was removed in 1.22.0. Project status is now a
 * single controlled meta field (`_rafah_status`) and NEVER changes the cover
 * image — status is shown as a CSS overlay instead (see
 * rafah_project_status_overlay_html() in includes/helpers.php).
 *
 * This file is intentionally left empty: it is no longer loaded by the plugin
 * bootstrap and can be deleted from disk. The class it used to define
 * (Rafah_Covers) no longer exists.
 *
 * @package Rafah_Core
 */

defined( 'ABSPATH' ) || exit;
