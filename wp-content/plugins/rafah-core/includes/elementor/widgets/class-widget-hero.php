<?php
/**
 * MOVED (v1.4.0): the Hero widget is presentation and now lives in the
 * Rafah THEME (`themes/rafah/inc/class-rafah-hero-widget.php`), registered
 * through the `rafah_core_widgets` filter. The widget name (`rafah_hero`)
 * is unchanged, so existing Elementor pages keep working.
 *
 * This file is intentionally inert and kept only so that in-flight requests
 * during an update never hit a missing include. Safe to delete.
 *
 * @package Rafah_Core
 * @deprecated 1.4.0 Moved to the Rafah theme.
 */

defined( 'ABSPATH' ) || exit;
